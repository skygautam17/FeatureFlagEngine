import { Routes } from '@angular/router';
import { AuditHistoryComponent } from '../../audit/audit-history.component';

export const AUDIT_ROUTES: Routes = [
  { path: '', component: AuditHistoryComponent }
];
